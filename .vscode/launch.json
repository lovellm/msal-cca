{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug Server",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "npm: build - server",
      "program": "${workspaceFolder}/index.ts",
      "outFiles": ["${workspaceFolder}/build/**/*.js"]
    }
  ]
}
